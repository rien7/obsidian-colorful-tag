export enum BooleanType {
    True = "True",
    False = "False"
}

export enum SizeType {
    px = "px",
    em = "em",
    percent = "%",
    rem = "rem"
}

export enum AttributeType {
    Text,
    Boolean,
    Color,
    Size,
    Number,
    Date,
    Any
}